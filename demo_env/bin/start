#!/bin/bash

path=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )

cd $path/../..
source source_me

cd $path/../vagrant
vagrant box update
vagrant up

cd $path/..
fleetctl start docker-registry
fleetctl start vulcand@{1..3} 

cd $path/../..
make release
bin/launch_service

